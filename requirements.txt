# Core dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Browser automation (Playwright-based scraping)
playwright>=1.40.0

# Database
# SQLite is built-in, no additional packages needed

# Security - encrypted credential storage
cryptography>=41.0.0
keyring>=24.0.0  # Optional: OS-level secure storage

# Optional: Excel export
# openpyxl>=3.1.0

# Development/Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0

# Optional: API server and n8n integration
# Uncomment to enable REST API:
# fastapi>=0.100.0
# uvicorn>=0.22.0

python-dateutil>=2.8.0  # Date parsing for various formats
